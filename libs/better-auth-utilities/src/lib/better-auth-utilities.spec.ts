import { betterAuthUtilities } from './better-auth-utilities';

describe('betterAuthUtilities', () => {
  it('should work', () => {
    expect(betterAuthUtilities()).toEqual('better-auth-utilities');
  });
});
