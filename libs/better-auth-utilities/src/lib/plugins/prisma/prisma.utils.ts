/**
 * @module prisma.utils
 * @description Provides utilities for interacting with the `better-auth` Prisma plugin.
 * These functions are designed using the Effect-TS functional programming paradigm.
 */

// AI-GENERATED FILE - DO NOT EDIT

// @ai-prompt
// Implement a utility function `initializePrismaAdapter` using Effect-TS.
// 1.  This function should be pure.
// 2.  It should accept a `PrismaClient` instance.
// 3.  It should call the `prismaAdapter` function from `better-auth/adapters/prisma`.
// 4.  Return the configured adapter.
// 5.  The function should return an `Effect` that resolves with the adapter instance.
// 6.  Follow the JSDoc template for pure functions.
/**
 * Initializes the better-auth Prisma adapter.
 *
 * @pure
 * @description This function takes a `PrismaClient` instance and configures the `better-auth`
 * Prisma adapter with it. It returns an `Effect` that provides the configured adapter.
 *
 * @param prisma - An instance of `PrismaClient`.
 * @returns {Effect.Effect<PrismaAdapter, never, never>} An `Effect` that resolves with the configured Prisma adapter.
 */
export const initializePrismaAdapter = (prisma: any) => {
  // AI, implement the function here.
};
